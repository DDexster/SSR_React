import React from 'react';

const Home = () => {
    return (
        <div>
            <div>You're home!... for now!</div>
            <button onClick={() => console.log("Hi!")}>PressMe</button>
        </div>
    )
}

export default Home;